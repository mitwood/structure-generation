# multi-element example

(To be moved to examples once functional)

This example is supposed to show that 
GRS reproduces simple SQS structures when the method is used on
a fixed lattice. It currently doesn't work (from what I can
tell because the candidates per generation aren't being 
updated/used correctly)
